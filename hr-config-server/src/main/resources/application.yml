server:
  port: 8888

spring:
  application:
    name: hr-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vinisolon/ms-course-configs.git
          user: ${GIT_USER}
          password: ${GIT_PASS}